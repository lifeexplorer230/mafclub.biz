# 📢 ИМПОРТ ПОСТОВ ИЗ TELEGRAM - ИНСТРУКЦИЯ

## 🎯 Как это работает

**Механизм аналогичен moderator.top:**

1. Python скрипт парсит публичную версию Telegram канала **@sellersmafia**
2. Извлекает последние 20 постов (текст, изображения, видео, ссылки, просмотры)
3. Сохраняет в JSON файл `telegram-posts.json`
4. Страница `blog.html` загружает JSON и красиво отображает посты
5. Автоматическое обновление через cron (каждые 3 часа)

**Не требуется:**
- Telegram Bot API token
- Доступ к каналу
- Серверное приложение

**Использует:**
- Публичную веб-версию канала (https://t.me/s/sellersmafia)
- Парсинг HTML через BeautifulSoup

---

## 🚀 БЫСТРЫЙ СТАРТ (5 минут)

### Шаг 1: Установите зависимости

```bash
cd /home/roman/mafclub.biz

# Установка Python библиотек
pip3 install requests beautifulsoup4 --user
```

### Шаг 2: Запустите первый импорт

```bash
# Первый импорт постов
python3 generate_telegram_posts.py
```

**Ожидаемый вывод:**
```
============================================================
📢 ИМПОРТ ПОСТОВ ИЗ TELEGRAM
============================================================
🔄 Парсинг канала: https://t.me/s/sellersmafia
✅ Найдено постов: 20
✅ JSON сохранен: telegram-posts.json
============================================================
🎉 ГОТОВО!
📊 Импортировано: 20 постов
📁 Файл: telegram-posts.json
============================================================
```

### Шаг 3: Проверьте результат локально

```bash
# Запустите локальный сервер
python3 -m http.server 8000
```

Откройте в браузере:
- http://localhost:8000/blog.html

Вы должны увидеть посты из канала @sellersmafia!

### Шаг 4: Задеплойте на GitHub Pages

```bash
cd /home/roman/mafclub.biz

# Добавляем файлы
git add blog.html telegram-posts.json generate_telegram_posts.py update_telegram_posts.sh

# Коммитим
git commit -m "feat(blog): добавлен импорт постов из Telegram @sellersmafia

- Скрипт парсинга канала (generate_telegram_posts.py)
- Страница блога с постами (blog.html)
- Автоматическое обновление (update_telegram_posts.sh)
- JSON с последними 20 постами

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>"

# Отправляем на GitHub
git push origin main
```

Подождите 2-5 минут, затем откройте: https://mafclub.biz/blog.html

---

## ⚙️ АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ

### Настройка Cron (обновление каждые 3 часа)

```bash
# Открываем crontab для редактирования
crontab -e

# Добавляем строку (обновление каждые 3 часа):
0 */3 * * * /home/roman/mafclub.biz/update_telegram_posts.sh >> /home/roman/mafclub.biz/telegram-update.log 2>&1

# Сохраняем и закрываем
```

**Расписание:**
- `0 */3 * * *` = каждые 3 часа (00:00, 03:00, 06:00, 09:00, 12:00, 15:00, 18:00, 21:00)
- Можно изменить на `0 */6 * * *` (каждые 6 часов)
- Или `0 9,15,21 * * *` (в 9:00, 15:00, 21:00)

### Ручное обновление

Если нужно обновить прямо сейчас:

```bash
cd /home/roman/mafclub.biz
./update_telegram_posts.sh
```

Или только парсинг без git push:

```bash
python3 generate_telegram_posts.py
```

### Просмотр логов

```bash
# Последние обновления
tail -f /home/roman/mafclub.biz/telegram-update.log

# Все логи
cat /home/roman/mafclub.biz/telegram-update.log
```

---

## 🔗 ДОБАВЛЕНИЕ ССЫЛКИ НА БЛОГ

### Вариант 1: Добавить кнопку в главное меню

Отредактируйте `index.html`, добавьте ссылку на блог:

```html
<a href="/blog.html" class="button">📢 Блог из Telegram</a>
```

### Вариант 2: Виджет "Последние посты" на главной

Добавьте на главную страницу секцию с последними 3 постами:

```html
<div style="padding:60px 0;text-align:center;">
  <h2>📢 Последние новости</h2>
  <p>Следите за обновлениями в нашем <a href="https://t.me/sellersmafia">Telegram канале</a></p>
  <a href="/blog.html" style="display:inline-block;margin-top:20px;padding:12px 30px;background:#ffc000;color:#000;text-decoration:none;border-radius:25px;font-weight:600;">
    Все посты →
  </a>
</div>
```

### Вариант 3: Обновить sitemap.xml

Добавьте blog.html в sitemap:

```xml
<url>
  <loc>https://mafclub.biz/blog.html</loc>
  <lastmod>2026-01-27</lastmod>
  <changefreq>daily</changefreq>
  <priority>0.8</priority>
</url>
```

---

## 🎨 КАСТОМИЗАЦИЯ

### Изменить количество постов

В файле `generate_telegram_posts.py` измените:

```python
MAX_POSTS = 20  # Измените на нужное число (например, 30)
```

### Изменить канал

Если хотите импортировать из другого канала:

```python
CHANNEL_URL = 'https://t.me/s/ВАШ_КАНАЛ'
```

В файле `blog.html` обновите ссылку:

```javascript
<a href="https://t.me/ВАШ_КАНАЛ" target="_blank">@ВАШ_КАНАЛ</a>
```

### Изменить дизайн

Редактируйте CSS в файле `blog.html`:
- Цвета: переменные CSS в `:root`
- Шрифты: `font-family`
- Размеры карточек: `.post-card`

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "Модуль не найден"

```bash
# Установите зависимости
pip3 install requests beautifulsoup4 --user

# Или через sudo (если не помогло выше)
sudo pip3 install requests beautifulsoup4
```

### Проблема: "Ошибка парсинга канала"

**Причины:**
1. Канал приватный (нужен публичный канал)
2. Нет интернета
3. Telegram заблокировал IP

**Решение:**
```bash
# Проверьте доступность
curl https://t.me/s/sellersmafia

# Если не работает, используйте VPN или прокси
```

### Проблема: "JSON не создается"

```bash
# Запустите с подробным выводом
python3 -u generate_telegram_posts.py

# Проверьте права на запись
ls -la telegram-posts.json
```

### Проблема: "Git push не работает"

```bash
# Проверьте статус Git
git status

# Проверьте remote
git remote -v

# Попробуйте вручную
git add telegram-posts.json
git commit -m "test"
git push origin main
```

### Проблема: "Cron не запускается"

```bash
# Проверьте логи cron
grep CRON /var/log/syslog | tail -20

# Проверьте права
chmod +x /home/roman/mafclub.biz/update_telegram_posts.sh

# Проверьте путь к скрипту
ls -la /home/roman/mafclub.biz/update_telegram_posts.sh

# Тестовый запуск
/home/roman/mafclub.biz/update_telegram_posts.sh
```

---

## 📊 МОНИТОРИНГ

### Проверка работы

```bash
# Когда последний раз обновлялось
stat telegram-posts.json

# Сколько постов в JSON
python3 -c "import json; print(json.load(open('telegram-posts.json'))['count'])"

# Дата последнего обновления
python3 -c "import json; print(json.load(open('telegram-posts.json'))['updated'])"
```

### Уведомления об ошибках

Добавьте в `update_telegram_posts.sh` отправку email при ошибке:

```bash
# В конце скрипта, если произошла ошибка:
if [ $? -ne 0 ]; then
    echo "Ошибка обновления блога" | mail -s "MafClub Blog Error" your@email.com
fi
```

---

## 🎯 ЧЕКЛИСТ

- [ ] Установлены зависимости Python (requests, beautifulsoup4)
- [ ] Первый импорт выполнен успешно
- [ ] JSON файл создан (telegram-posts.json)
- [ ] Страница blog.html открывается локально
- [ ] Файлы задеплоены на GitHub Pages
- [ ] blog.html доступен на https://mafclub.biz/blog.html
- [ ] Настроен cron для автообновления
- [ ] Добавлена ссылка на блог на главной странице
- [ ] blog.html добавлен в sitemap.xml
- [ ] Логи проверяются регулярно

---

## 📚 ФАЙЛЫ ПРОЕКТА

```
mafclub.biz/
├── blog.html                       # Страница блога
├── telegram-posts.json             # JSON с постами (генерируется)
├── generate_telegram_posts.py      # Скрипт парсинга канала
├── update_telegram_posts.sh        # Автообновление с git push
├── telegram-update.log             # Логи обновлений (создается)
└── TELEGRAM_IMPORT_SETUP.md        # Эта инструкция
```

---

## 🎉 ГОТОВО!

Теперь у вас работает автоматический импорт постов из Telegram, как на moderator.top!

**Что дальше:**
- Добавьте ссылку на блог на главную страницу
- Настройте регулярные обновления через cron
- Расскажите подписчикам о новом блоге
- Мониторьте логи первые несколько дней

**Вопросы?** Просто скажите что нужно! 🚀
